<div class="container-fluid slice">
  <div class="container align-self-center">
    <div class="form-horizontal">
      <br>
      <div class="form-group">
        <label class="col-sm-2 control-label">Title</label>
        <div class="col-sm-10">
          {{input type="text" value=newTitle class="form-control" placeholder="Insert Article Title..."}}
        </div>
      </div>
      <div class="form-group">
        <label class="col-sm-2 control-label">Date</label>
        <div class="col-sm-10">
          {{input type="date" value=newDate class="form-control"}}
        </div>
      </div>
      <div class="form-group">
        <label class="col-sm-6 control-label">Image-Thumbnail (Drag & Drop Here)</label>
        <div class="col-sm-6">
          {{tinymce-editor options=imageOptions value=newImage}}
        </div>
      </div>
      <div class="form-group">
        <label class="col-sm-2 control-label">Article Content</label>
        <div class="col-sm-12">
          {{tinymce-editor options=ownOptions value=newContent}}
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" class="btn btn-default" {{action "saveArticle"}}>Add to Article List</button>
        </div>
      </div>
    </div>

    {{#if responseMessage}}
      <div class="alert alert-success">{{responseMessage}}</div>
    {{/if}}

  </div>
</div>

{{outlet}}