import short from 'karir-admin/utils/short';
import { module, test } from 'qunit';

module('Unit | Utility | short', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = short();
    assert.ok(result);
  });
});
